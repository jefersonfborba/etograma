# Story 1.3: Configurar permissões e recursos nativos Android

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["build-validation", "permission-audit"]

## Story
**As a** usuário do Etograma no Android,
**I want** que o app solicite as permissões corretas e apareça com ícone e identidade visual adequados,
**so that** a experiência nativa seja profissional e todas as funcionalidades (microfone, storage) funcionem sem erros de permissão.

## Acceptance Criteria

1. O `AndroidManifest.xml` contém a permissão `RECORD_AUDIO` (necessária para `getUserMedia`)
2. O `AndroidManifest.xml` contém as permissões de storage adequadas para Android 9–14
3. O app aparece com ícone correto (192×192 adaptativo) na tela inicial do Android
4. O app aparece com nome "Etograma" no launcher do Android
5. Uma splash screen é configurada (cor sólida verde `#1a3c1f`, sem logo por ora)
6. O `targetSdkVersion` está em 34 (Android 14) e `minSdkVersion` em 24 (Android 7)
7. A orientação do app é forçada para `portrait` no `AndroidManifest.xml`
8. O build `npx cap sync` executa sem warnings de configuração

## 🤖 CodeRabbit Integration

**Story Type Analysis:**
- **Primary Type:** Deployment/Infrastructure
- **Secondary Type(s):** Security (permissões)
- **Complexity:** Low — edição de arquivos de configuração Android

**Specialized Agent Assignment:**
- Primary Agents:
  - @dev (configuração dos arquivos Android)
  - @architect (auditoria de permissões — princípio de mínimo privilégio)
- Supporting Agents:
  - N/A

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): `npx cap sync` sem erros após mudanças
- [ ] Pre-PR (@devops): Validar permissões no dispositivo (Story 1.4)

**Self-Healing Configuration:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutos
- Severity Filter: CRITICAL

**CodeRabbit Focus Areas:**
- Primary Focus:
  - Permissões seguindo o princípio de mínimo privilégio
  - Sem permissões desnecessárias no `AndroidManifest.xml`
- Secondary Focus:
  - `targetSdkVersion` compatível com Play Store requirements (34+)
  - Sem credenciais ou chaves hardcoded nos arquivos de configuração Android

## Tasks / Subtasks

- [ ] Task 1: Configurar permissões no `AndroidManifest.xml` (AC: 1, 2)
  - [ ] Abrir `android/app/src/main/AndroidManifest.xml`
  - [ ] Adicionar `<uses-permission android:name="android.permission.RECORD_AUDIO" />`
  - [ ] Adicionar `<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />` (Android ≤ 12)
  - [ ] Adicionar `<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />` (Android ≤ 9)
  - [ ] Verificar que `INTERNET` e `ACCESS_NETWORK_STATE` estão presentes (padrão Capacitor)

- [ ] Task 2: Configurar orientação portrait (AC: 7)
  - [ ] No `AndroidManifest.xml`, adicionar `android:screenOrientation="portrait"` na activity principal

- [ ] Task 3: Configurar SDK versions no `build.gradle` (AC: 6)
  - [ ] Abrir `android/app/build.gradle`
  - [ ] Verificar/ajustar `compileSdkVersion 34`, `targetSdkVersion 34`, `minSdkVersion 24`

- [ ] Task 4: Configurar ícone do app (AC: 3)
  - [ ] Gerar ícones Android nas densidades necessárias a partir de `icons/icon-512.png`:
    - `mipmap-mdpi`: 48×48
    - `mipmap-hdpi`: 72×72
    - `mipmap-xhdpi`: 96×96
    - `mipmap-xxhdpi`: 144×144
    - `mipmap-xxxhdpi`: 192×192
  - [ ] Colocar em `android/app/src/main/res/mipmap-{density}/ic_launcher.png`
  - [ ] Verificar referência no `AndroidManifest.xml`: `android:icon="@mipmap/ic_launcher"`

- [ ] Task 5: Configurar splash screen (AC: 5)
  - [ ] Adicionar cor de fundo no `android/app/src/main/res/values/styles.xml`
  - [ ] Configurar `@capacitor/splash-screen` ou usar background color nativo (`#1a3c1f`)
  - [ ] Verificar que splash desaparece após app carregar

- [ ] Task 6: Configurar nome do app (AC: 4)
  - [ ] Verificar `android/app/src/main/res/values/strings.xml`: `<string name="app_name">Etograma</string>`

- [ ] Task 7: Sync e verificação final (AC: 8)
  - [ ] Executar `npx cap sync`
  - [ ] Verificar que não há warnings de configuração na saída

## Dev Notes

### Permissões por versão do Android

| Permissão | Android ≤ 9 | Android 10-12 | Android 13-14 |
|-----------|-------------|---------------|---------------|
| `WRITE_EXTERNAL_STORAGE` | Obrigatória | Limitada | Não existe |
| `READ_EXTERNAL_STORAGE` | Obrigatória | Obrigatória | Substituída por `READ_MEDIA_*` |
| `RECORD_AUDIO` | Obrigatória | Obrigatória | Obrigatória |

> Para exportação via `@capacitor/filesystem` com `Directory.Cache`, **não** são necessárias permissões de storage no Android 10+. As permissões de storage são relevantes apenas se o app precisar ler arquivos externos.

### Localização dos arquivos Android

```
android/
├── app/
│   ├── build.gradle                          ← SDK versions
│   └── src/main/
│       ├── AndroidManifest.xml               ← permissões, orientação
│       ├── res/
│       │   ├── mipmap-*/ic_launcher.png      ← ícones por densidade
│       │   └── values/
│       │       ├── strings.xml               ← app_name
│       │       └── styles.xml                ← splash screen
```

### Exemplo de `AndroidManifest.xml` após configuração

```xml
<manifest>
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.RECORD_AUDIO" />
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
    android:maxSdkVersion="32" />
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
    android:maxSdkVersion="29" />

  <application
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name">
    <activity
      android:screenOrientation="portrait"
      ...>
    </activity>
  </application>
</manifest>
```

### Geração de ícones

Usar ImageMagick (WSL) ou ferramenta online para redimensionar `icons/icon-512.png`:

```bash
# WSL/Linux
convert icons/icon-512.png -resize 48x48 android/app/src/main/res/mipmap-mdpi/ic_launcher.png
convert icons/icon-512.png -resize 72x72 android/app/src/main/res/mipmap-hdpi/ic_launcher.png
convert icons/icon-512.png -resize 96x96 android/app/src/main/res/mipmap-xhdpi/ic_launcher.png
convert icons/icon-512.png -resize 144x144 android/app/src/main/res/mipmap-xxhdpi/ic_launcher.png
convert icons/icon-512.png -resize 192x192 android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png
```

Alternativa: `npx @capacitor/assets generate` se `@capacitor/assets` for instalado.

### Dependência

Esta story **depende da Story 1.1** (Capacitor e pasta `android/` devem existir).

### Fonte da decisão arquitetural

Identificado por @architect em 2026-06-08: `getUserMedia` requer `RECORD_AUDIO`; `blob:` download substituído por Filesystem + Share (Story 1.2).

## Testing

- `npx cap sync` sem erros
- Verificação visual do ícone: conferir `mipmap-*/ic_launcher.png` nos tamanhos corretos
- Verificação de orientação: será validada no dispositivo real (Story 1.4)
- Permissões: serão validadas no dispositivo real durante Story 1.4

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-06-08 | 1.0 | Story criada | River (@sm) |
