# Story 1.2: Adaptar exportação CSV para Android

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["code-review", "device-test"]

## Story
**As a** pesquisador usando o Etograma no Android,
**I want** exportar meus dados em CSV normalmente,
**so that** eu possa analisar os registros etológicos no computador, mesmo com o app instalado como APK nativo.

## Acceptance Criteria

1. A exportação de CSV funciona no app Android (APK instalado via sideloading)
2. O fluxo de exportação usa `@capacitor/filesystem` para salvar o arquivo e `@capacitor/share` para abrir o compartilhamento nativo do Android
3. No ambiente web (browser), o comportamento original de download via `blob:` URL é preservado
4. O arquivo CSV exportado tem o nome correto (ex: `etograma-{projeto}-{data}.csv`)
5. Após compartilhar, o usuário pode salvar o arquivo em Google Drive, WhatsApp, email, etc.
6. Se `@capacitor/share` não estiver disponível (web), o fallback para `blob:` é automático

## 🤖 CodeRabbit Integration

**Story Type Analysis:**
- **Primary Type:** Frontend
- **Secondary Type(s):** Integration
- **Complexity:** Medium — detecção de ambiente + duas implementações de exportação

**Specialized Agent Assignment:**
- Primary Agents:
  - @dev (implementação)
  - @architect (revisão do padrão de detecção de ambiente)
- Supporting Agents:
  - N/A

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): Testar exportação no browser (fallback deve continuar funcionando)
- [ ] Pre-PR (@devops): Validar no dispositivo Android físico

**Self-Healing Configuration:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutos
- Severity Filter: CRITICAL

**CodeRabbit Focus Areas:**
- Primary Focus:
  - Detecção de ambiente Capacitor vs browser sem hardcode de plataforma
  - Fallback garantido para ambiente web sem erros
- Secondary Focus:
  - Permissões de filesystem não devem ser pedidas desnecessariamente no web
  - Nome do arquivo CSV consistente com o comportamento atual

## Tasks / Subtasks

- [ ] Task 1: Instalar plugins Capacitor (AC: 2)
  - [ ] `npm install @capacitor/filesystem @capacitor/share`
  - [ ] `npx cap sync`

- [ ] Task 2: Identificar a função de exportação atual no `etograma.html` (AC: 1)
  - [ ] Localizar a função de exportação CSV no código (buscar por `blob`, `URL.createObjectURL`, `download`)
  - [ ] Documentar o fluxo atual de geração e download do CSV

- [ ] Task 3: Implementar detecção de ambiente (AC: 3, 6)
  - [ ] Criar função `isNativeApp()` que detecta contexto Capacitor via `window.Capacitor?.isNativePlatform()`
  - [ ] Garantir que a função retorna `false` no browser normalmente

- [ ] Task 4: Implementar exportação nativa (AC: 2, 4, 5)
  - [ ] Importar `Filesystem`, `Directory`, `Share` do Capacitor
  - [ ] Implementar `exportCsvNative(csvContent, fileName)`:
    - Salvar em `Directory.Cache` com `Filesystem.writeFile`
    - Abrir share sheet com `Share.share({ url: filePath })`

- [ ] Task 5: Adaptar função de exportação existente (AC: 3, 6)
  - [ ] Modificar a função original para verificar `isNativeApp()`
  - [ ] Se nativo → chamar `exportCsvNative()`
  - [ ] Se web → manter comportamento atual com `blob:` URL

- [ ] Task 6: Atualizar `www/index.html` (AC: 1)
  - [ ] Copiar `etograma.html` atualizado para `www/index.html`
  - [ ] Executar `npx cap sync`

- [ ] Task 7: Teste no browser (AC: 3, 6)
  - [ ] Verificar que a exportação via browser continua funcionando identicamente

## Dev Notes

### Padrão de detecção de ambiente

```javascript
function isNativeApp() {
  return !!(window.Capacitor && window.Capacitor.isNativePlatform());
}
```

### Implementação de exportação nativa

```javascript
import { Filesystem, Directory } from '@capacitor/filesystem';
import { Share } from '@capacitor/share';

async function exportCsvNative(csvContent, fileName) {
  // Salva no cache temporário
  const result = await Filesystem.writeFile({
    path: fileName,
    data: csvContent,
    directory: Directory.Cache,
    encoding: 'utf8'
  });

  // Abre o share sheet nativo do Android
  await Share.share({
    title: 'Exportar dados etograma',
    url: result.uri,
  });
}
```

### Função adaptada (padrão recomendado)

```javascript
async function exportarCSV() {
  const csvContent = gerarCSV(); // função existente
  const fileName = `etograma-${nomeProjetoAtual}-${formatarData()}.csv`;

  if (isNativeApp()) {
    await exportCsvNative(csvContent, fileName);
  } else {
    // Comportamento web original — preservado
    const blob = new Blob([csvContent], { type: 'text/csv;charset=utf-8;' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = fileName;
    a.click();
    URL.revokeObjectURL(url);
  }
}
```

### Localização no código

A função de exportação está em `etograma.html`. Buscar por `blob`, `URL.createObjectURL` ou `download` para localizar. O código é inline — a modificação deve ser feita diretamente no arquivo e depois copiada para `www/index.html`.

### Dependência da Story 1.1

Esta story **depende da Story 1.1** — a estrutura `www/` e o Capacitor devem estar configurados antes.

### Fonte do gap arquitetural

Identificado por @architect em 2026-06-08: `blob:` URLs não funcionam no contexto nativo do Capacitor. Requer Filesystem + Share plugins.

## Testing

- **No browser:** Verificar que download CSV continua funcionando normalmente (fallback)
- **No Android (Story 1.4):** Verificar que share sheet abre com o arquivo CSV correto
- **Arquivo gerado:** Verificar encoding UTF-8 e separadores corretos no CSV

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-06-08 | 1.0 | Story criada | River (@sm) |
